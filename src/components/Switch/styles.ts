import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 50,
    height: 20,
    backgroundColor: "red",
    borderRadius: 100,
    padding: 2.5
},
  toggle: {
    width: 15,
    height: 15,
    backgroundColor: "blue",
    borderRadius: 100
  }
});
